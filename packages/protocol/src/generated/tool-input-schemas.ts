// generated by codegen.mjs — do not edit
// Source of truth: packages/protocol/schemas/**
// Regenerate with: pnpm --filter @xplainer/protocol codegen

import type { ToolName } from "./manifest.js";

/** A JSON Schema document, opaque to TypeScript and read by whatever validates. */
export type ToolInputSchema = Readonly<Record<string, unknown>>;

/**
 * Each tool's input schema with every cross-file `$ref` already inlined.
 *
 * This is what a surface publishes in `tools/list`. The unbundled documents in
 * `schemas/tools/` remain the contract; these are the same documents made
 * self-contained, because a client receiving one schema has no base URI to
 * resolve `../narration.json` against and would be handed a broken pointer.
 *
 * `not` is absent by construction — see `bundleToolInput` in codegen.mjs.
 */
export const TOOL_INPUT_SCHEMAS: Readonly<Record<ToolName, ToolInputSchema>> = Object.freeze({
  "explainer_create": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerCreateInput",
  "description": "Arguments for explainer_create.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug"
  ],
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Identifier for one explainer video. Lowercase letters, digits and hyphens; must start with a letter or digit; at most 64 characters. Ported verbatim from the reference implementation's SLUG_RE, because the slug is also a directory name on every backend.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"
    }
  }
},
  "explainer_put_source": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerPutSourceInput",
  "description": "Arguments for explainer_put_source: upload agent-authored Remotion source into an existing video. Exists because an agent talking to a hosted backend has no disk to write to.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "files"
  ],
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Identifier for one explainer video. Lowercase letters, digits and hyphens; must start with a letter or digit; at most 64 characters. Ported verbatim from the reference implementation's SLUG_RE, because the slug is also a directory name on every backend.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"
    },
    "files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/SourceFile"
      },
      "description": "Files to write, overwriting any file of the same path. The engine-owned files are refused; see the path constraint."
    }
  },
  "$defs": {
    "SourceFile": {
      "title": "SourceFile",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "content"
      ],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9_-][A-Za-z0-9._-]*(/[A-Za-z0-9_-][A-Za-z0-9._-]*)*$",
          "description": "A relative path built from plain segments. No leading slash, and no segment may begin with a dot, so \"..\" cannot appear and a write can never escape the video directory. The pattern is deliberately lookahead-free: pydantic v2 compiles it with the Rust regex engine, which has no lookaround. The `not`/`enum` reserves the five engine-owned files listed in manifest.json's engine_owned_files: index.ts, types.ts, Root.tsx, Captions.tsx and Video.tsx mount the narration audio, the caption track and the per-segment sequencing, so an agent that rewrote one could ship a silent video. Only those exact top-level paths are reserved — a nested scenes/Root.tsx is the agent's and stays writable. `not`/`enum` rather than a negative lookahead for the same Rust-regex reason. Both code generators ignore `not`, so this is the published contract and the ajv gate, not the runtime gate; the runtime gate is assertAgentOwnedPaths() in @xplainer/mcp-server."
        },
        "content": {
          "type": "string",
          "description": "UTF-8 file contents."
        }
      }
    }
  }
},
  "explainer_put_media": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerPutMediaInput",
  "description": "Arguments for explainer_put_media: upload one image, audio or video asset the composition will reference through staticFile().",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "name",
    "base64"
  ],
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Identifier for one explainer video. Lowercase letters, digits and hyphens; must start with a letter or digit; at most 64 characters. Ported verbatim from the reference implementation's SLUG_RE, because the slug is also a directory name on every backend.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"
    },
    "name": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
      "description": "Filename inside the video's media directory. A bare name, never a path."
    },
    "base64": {
      "type": "string",
      "contentEncoding": "base64",
      "description": "The asset's bytes, base64-encoded."
    }
  }
},
  "explainer_narrate": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerNarrateInput",
  "description": "Arguments for explainer_narrate. Shape ported from the reference implementation, minus the session_name argument, which was its internal job-routing detail and is not part of this contract.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "narration"
  ],
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Identifier for one explainer video. Lowercase letters, digits and hyphens; must start with a letter or digit; at most 64 characters. Ported verbatim from the reference implementation's SLUG_RE, because the slug is also a directory name on every backend.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"
    },
    "narration": {
      "title": "Narration",
      "description": "The narration spec an agent hands to explainer_narrate. Field defaults mirror the reference implementation that consumes this document.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "segments"
      ],
      "properties": {
        "voice": {
          "type": "string",
          "default": "af_heart",
          "description": "Kokoro voice id, or a blend such as \"af_bella(2)+af_sky(1)\". Blends are resolved server-side."
        },
        "speed": {
          "type": "number",
          "default": 1,
          "minimum": 0.25,
          "maximum": 4,
          "description": "Speech rate multiplier passed straight through to Kokoro."
        },
        "fps": {
          "type": "integer",
          "default": 30,
          "minimum": 1,
          "description": "Frame rate the measured timings are converted to. Must match the composition."
        },
        "langCode": {
          "type": "string",
          "description": "Optional Kokoro language code. Inferred from the voice when omitted."
        },
        "segments": {
          "type": "array",
          "minItems": 1,
          "description": "Ordered narration segments. One timings entry is produced per segment, in this order.",
          "items": {
            "$ref": "#/$defs/NarrationSegment"
          }
        }
      }
    },
    "dry_run": {
      "type": "boolean",
      "default": false,
      "description": "Emit silence of a plausible length instead of calling the TTS server. Produces real timings.json and captions.json, so a composition can be built and checked before any speech is synthesised."
    }
  },
  "$defs": {
    "NarrationSegment": {
      "title": "NarrationSegment",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "text"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable scene identifier. Reappears as the id of the matching timings segment."
        },
        "text": {
          "type": "string",
          "description": "What the voice says. Empty text yields a silent segment of holdSeconds length."
        },
        "holdSeconds": {
          "type": "number",
          "minimum": 0,
          "description": "Minimum on-screen duration. Silence is appended when the narration is shorter, so an animation can finish or a beat can land."
        }
      }
    }
  }
},
  "explainer_still": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerStillInput",
  "description": "Arguments for explainer_still. Shape and defaults ported from the reference implementation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug"
  ],
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Identifier for one explainer video. Lowercase letters, digits and hyphens; must start with a letter or digit; at most 64 characters. Ported verbatim from the reference implementation's SLUG_RE, because the slug is also a directory name on every backend.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"
    },
    "frame": {
      "type": "integer",
      "minimum": 0,
      "default": 90,
      "description": "Frame number to render."
    },
    "scale": {
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 1,
      "default": 0.5,
      "description": "Output scale. Half size is enough for a layout check and much faster."
    }
  }
},
  "explainer_render": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerRenderInput",
  "description": "Arguments for explainer_render. Picture, voiceover and captions are produced in one pass, so there is no ffmpeg step and no options to choose between.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug"
  ],
  "properties": {
    "slug": {
      "title": "Slug",
      "description": "Identifier for one explainer video. Lowercase letters, digits and hyphens; must start with a letter or digit; at most 64 characters. Ported verbatim from the reference implementation's SLUG_RE, because the slug is also a directory name on every backend.",
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$"
    }
  }
},
  "explainer_job": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerJobInput",
  "description": "Arguments for explainer_job. Shape and default ported from the reference implementation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "job_id"
  ],
  "properties": {
    "job_id": {
      "type": "integer",
      "description": "The id returned by explainer_narrate, explainer_render or explainer_still."
    },
    "output_lines": {
      "type": "integer",
      "minimum": 1,
      "default": 60,
      "description": "How many trailing output lines to return."
    }
  }
},
  "explainer_list": {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExplainerListInput",
  "description": "explainer_list takes no arguments: it lists everything the caller owns on the backend it is talking to.",
  "type": "object",
  "additionalProperties": false,
  "properties": {}
},
});
