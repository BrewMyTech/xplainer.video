{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "JobState",
  "description": "Lifecycle state of a queued explainer job. The five values are the ones the reference implementation's job queue reports and are the complete set: a job is always in exactly one of them.",
  "type": "string",
  "enum": ["queued", "running", "done", "error", "cancelled"]
}
